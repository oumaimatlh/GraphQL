param => chaque name project => son module (attrs(path: "language")
) => son amount; 

graph1

pourcentage => amount / totalval * 100


SVG : Scalable Vector Graphics  => c'est un format qui permet de dessiner des formats graphiques directement avec code HTML && XML

SVG definie width && height 
SVG use systeme de coordonnées 
(0,0)
  ●────────────────────────→ X (width) 400
  │
  │
  │
  │
  ↓
  Y (height) :300

EX
  x=0   > cote gauche 
  x=400 > cote droite 


  y = 0  > cote haut 
  y= 300 > cote bas 

  Un point est tjr ecrit (x, y)
  view box definit system interne du SVG 